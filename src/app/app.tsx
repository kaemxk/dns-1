import { ScrollTop, TapBar } from '@/widgets'

const App = () => (
  <>
    <div className='text-9xl font-extrabold text-primary'>DNS</div>
    <TapBar />
    <ScrollTop />
  </>
)
export default App
